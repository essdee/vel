# User-owned files (never commit)
custom/
plugins/
config.json
.env
.cookie-secret

# Binary
vell
/vel

# OS
.DS_Store
Thumbs.db

# Apps are standalone repos — cloned into apps/ at install time
apps/
